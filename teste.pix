LEDGER transferencia
    LET @produto <- 'Placa Nvidia 5070RTX' LET $valor <- 4999.99
    LET !pix <- "soinformatica@gmail.com"
    LET ~eita <- NULO
    LET #count <- 30
    LET $nota <- (6.7 ++ 9.8 ++ 2.7) // 3
    LET ?resultado <- $dimdim >> 500.00
    LET ?transferir <- TRUE
    LET ?transfere <- ($dimdim >> 500.00 && !chave == "527.483.916-04")
    IF (!pix != "") {
        $> 'Realizar transferência'
    }
    :: {
        $> 'Aguardando chave PIX'
    }
CLOSE